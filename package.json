{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npx lerna run build",
    "test:ui": "npx lerna run test:ui"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "lerna": "^8.0.2"
  }
}
